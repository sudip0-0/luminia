// Articles module barrel — Feed_Service article detail and related articles.
//
// Exposes the article-detail / related-article retrieval service
// (`getArticleDetail`, `getRelatedArticles`), the injected data-access surface
// and its repository-backed factory, and the discriminated result types
// (Requirements 11.1-11.4, 20.1, 20.3).

export * from './detail.js';
