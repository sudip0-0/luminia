// Typesense module barrel — the pure `articles` collection schema and field
// definitions, the article-to-document mapping, the TypesenseLike interface,
// the live-client adapter, and the ArticlesIndex wrapper.
// See the "Typesense Collection (`articles`)" section in the design document.

export * from './schema.js';
export * from './document.js';
export * from './client.js';
