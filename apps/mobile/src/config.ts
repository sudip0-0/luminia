// Static client configuration for the Lumina Mobile_App.

export const APP_NAME = 'Lumina';

/** Default API base URL; overridden per environment in later tasks. */
export const DEFAULT_API_BASE_URL = 'http://localhost:3000';
